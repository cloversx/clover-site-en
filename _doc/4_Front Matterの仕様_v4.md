* * *

📘 Front Matter 仕様（Clover Canonical Model / 統合最終版）
==================================================

0. 前提と設計原則
-----------

### 0.1 正典（canonical）

*   正典は **GitHub Pages（EN repo / JA repo）** 上のページ。
*   Hub やミラー（Medium / note / Substack 等）は正典ではない。

### 0.2 URLと構造識別子

*   URLは **ディレクトリ構造（= `page.url`）** によって決まる。
*   構造識別子として **`/0/`** を採用する。
*   現行版とアーカイブのURL構造：

```
/0/<dir-name>/
 /0/<dir-name>/v1/
 /0/<dir-name>/v2/
 ...
```

> 注意：`<dir-name>` は「ディレクトリ名」。  
> front matter の `content_id` は **URL生成には使わない**（論理ID）。  
> ただし運用上は **ディレクトリ名と一致させることを推奨**する。

### 0.3 表示名と構造の分離

*   表示名（例：Library / Archive / Atlas）は **ナビゲーション表示**であり、URLとは独立。
*   したがって URL に essay 等の意味語を埋め込まない。

### 0.4 repoと言語の前提（重要）

*   **英語版と日本語版はリポジトリで分離**されている。
*   よって「ページの自己言語」は repo の `site.lang` が真実であり、**hreflangの self は `site.lang` を信頼する**。

* * *

1. フィールド一覧（型・説明・備考つき）
----------------------

### 1.1 必須（Required）

| フィールド | 型 | 説明 | 備考 |
| --- | --- | --- | --- |
| `layout` | string | 使用レイアウト | 現在は `"content"` を想定（旧: `"essay"`） |
| `title` | string | タイトル | SEO/OG/一覧表示に使用 |
| `is_archive` | boolean | アーカイブ判定 | 現行 `false` / アーカイブ `true` |
| `version` | number | バージョン番号 | **整数**（1,2,3…）思想段階 |
| `content_id` | string | 論理識別子（永続ID） | **URLとは独立**。現行/アーカイブを通じて変更しない |

#### content_id の補足（重要）

*   `content_id` は「同一コンテンツの全バージョンを貫く論理ID」。
*   アーカイブして `/vN/` に移っても **content_id は変更しない**。
*   URLはディレクトリで決まるので、`content_id` は技術的に一致不要だが、運用上は一致推奨。

* * *

### 1.2 強く推奨（Strongly recommended：SEO・多言語・メタ）

| フィールド | 型 | 説明 | 備考 |
| --- | --- | --- | --- |
| `lang` | string | 言語コード | `"en"` / `"ja"` 推奨。未指定時は `site.lang` にフォールバック可能 |
| `canonical_url` | string | 正規URL | **未指定/空なら自動生成**。例外時のみ手入力 |
| `alt_url` | string | 他言語の現行版URL | 対応がある場合のみ。空文字OK |
| `summary` | string | SEO/OG/RSS要約 | あると強い（空でも可） |
| `first_published` | date | 初回公開日 | **versionが上がっても固定** |
| `last_updated` | date | 最終更新日 | 更新ごとに更新 |

* * *

### 1.3 任意（Optional：表示・運用・将来拡張）

| フィールド | 型 | 説明 | 備考 |
| --- | --- | --- | --- |
| `tags` | array[string] | タグ | 現状は表示のみ。分類ページは後で |
| `featured` | boolean | Featured表示 | トップ/一覧で強調 |
| `featured_rank` | number | Featuredの順序 | 小さいほど上 |
| `og_image` | string | OG画像パス | 未指定時はサイト既定にフォールバック |
| `redirect_from` | array[string] | 旧URLからの誘導 | URL変更時に使う（今は不要でもOK） |
| `content_type` | string | 種別 | 当面は不要。タグ運用優先 |
| `status` | string | 公開状態 | 当面は不要（ローカルで完成→公開） |

* * *

2. canonical_url の仕様（自動生成と例外）
-------------------------------

### 2.1 自動生成（通常）

*   `canonical_url` が `nil` または `""` の場合、テンプレート側で自動生成：

```
canonical = site.url + site.baseurl + page.url
```

*   これにより現行版もアーカイブも、それぞれ **自分自身が canonical** になる。

### 2.2 例外（手入力が必要なケース）

次の場合は `canonical_url` を明示入力する必要がある：

*   正典ホスト（ドメイン）移転を行う
*   URL構造を変更する（/0/以外へ移行等）
*   正典を別ホストに統合する
*   SEO上の統合都合で canonical を固定したい

* * *

3. hreflang と lang の仕様（現状実装に合わせた記述）
------------------------------------

### 3.1 hreflang の自己言語（self）の真実

*   EN/JAが repo分離されているため、自己言語は **`site.lang` が真実**。
*   したがって `hreflang` の self は **`site.lang` を使用**する。
*   これは「page側の `lang` 記載ミスで hreflang が壊れる」事故を避けるため。

### 3.2 lang のフォールバック

*   `lang` は front matter として推奨される。
*   未指定の場合でもテンプレ側の表示や判定で：

```
page.lang | default: site.lang
```

のようにフォールバック可能。

> ただし hreflang self は現状 `site.lang` を信頼するため、page.lang 未指定でも hreflang self は正しく出る。

### 3.3 lang を明示する意味（それでも推奨する理由）

repo分離であっても `lang` を入れておく価値はある：

*   将来、JSON index や検索・フィルタ等を作る際にページ単位の言語情報として使える
*   コンテンツ属性として「仕様が自己完結」する（データ整合）
*   将来もし repo 方針が変わっても移行しやすい

結論：  
**hreflang self は site.lang、lang は推奨（データ整合・将来用途）**。

* * *

4. alt_url の運用方針（現行/アーカイブ）
----------------------------

### 4.1 現行版

*   対応記事がある場合のみ `alt_url` を入れる。
*   `alt_url` は相手言語の **現行版** を指す（アーカイブ同士のリンクは基本しない）。

### 4.2 アーカイブ版（/vN/）

アーカイブにコピーする際：

*   `alt_url` は **基本的に変更しない**（運用を軽くするため）
*   テンプレート側で `is_archive == true` の場合、hreflang/相互リンク出力を抑制する前提

運用の明快さ重視なら：

*   アーカイブは `alt_url: ""` で統一してもよい

どちらでも整合性は保たれる。

* * *

5. summary の運用方針（現行/アーカイブ）
---------------------------

*   `summary` は基本的に **アーカイブ化しても変更しない**。
*   理由：summary はコンテンツ要約であり、版が凍結されても要約として意味を保つことが多い。

ただし運用上の理由で変えたい場合は変更してよい。例：

*   `"Archived snapshot of Version 1."`

* * *

6. version / 日付の意味（運用定義）
-------------------------

| 項目 | 意味 |
| --- | --- |
| `version` | 思想段階（1,2,3…） |
| Minor update | 同一version内の軽微修正（誤字など） |
| `first_published` | 最初に公開された日（固定） |
| `last_updated` | 現行版が最後に更新された日 |
| アーカイブ | 過去の思想状態の凍結保存（内容は変更しない） |

* * *

7. 例（/0移行後）
------------

### 7.1 現行版（EN）

```yaml
---
layout: content
title: "AI Isn’t Dangerous. Putting AI Inside an Evaluation Structure Is."
lang: en

content_id: ai-evaluation-structure
is_archive: false
version: 2

canonical_url: ""  # 通常は省略 or 空でOK（自動生成）
alt_url: "https://cloversx.github.io/clover-site-ja/0/ai-evaluation-structure/"

summary: "AI isn’t inherently dangerous—but evaluation-driven environments can amplify instability."
tags: [ai-safety, system-design]

first_published: 2026-02-06
last_updated: 2027-05-10

featured: true
featured_rank: 1
---
```

### 7.2 アーカイブ（EN / v1）

```yaml
---
layout: content
title: "AI Isn’t Dangerous. Putting AI Inside an Evaluation Structure Is."
lang: en

content_id: ai-evaluation-structure   # 変更しない
is_archive: true
version: 1

canonical_url: ""  # 自動生成で /v1/ を含む canonical になる
alt_url: "https://cloversx.github.io/clover-site-ja/0/ai-evaluation-structure/"  # 変更しない（出力されない前提）

summary: "AI isn’t inherently dangerous—but evaluation-driven environments can amplify instability."
first_published: 2026-02-06
last_updated: 2026-02-06
---
```

* * *

8. テンプレ側で参照される主な変数（想定）
-----------------------

*   `page.title`
*   `page.summary`（description/OG/Twitter/RSS）
*   `page.is_archive`
*   `page.version`
*   `page.first_published`
*   `page.last_updated`
*   `page.alt_url`（現行のみ）
*   `page.lang`（表示/将来用途、未指定時は `site.lang` 想定）
*   `page.featured` / `page.featured_rank`
*   `page.tags`
*   `page.canonical_url`（例外時のみ）

* * *

🔁 変更履歴（v1→v2→v3→v3.1→現状）
=========================

v1（初期）
------

*   `/essays/<slug>/` 構造
*   `slug` 使用
*   `version_label` と `archive_version` が存在

v2（整理）
------

*   `canonical_url` 自動生成方針を明確化
*   featured / tags / og_image / redirect_from など任意項目を整理

v3（設計転換）
--------

*   構造識別子 `/0/` 導入（表示名と構造の分離）
*   `slug` → `content_id` へ移行方針
*   `version` を整数モデルへ統一
*   `archive_version` / `version_label` 廃止

v3.1（表現整理）
----------

*   `content_id` はURLとは独立（ただし一致推奨）を明示
*   `essay` など意味語をURL構造に組み込まない方針を明確化

現状（追加反映）
--------

*   hreflang self は repo の `site.lang` を信頼（page.lang参照しない）  
    → page側ミスによる誤出力を防ぐ
*   `lang` はフォールバック可能だが、将来用途・データ整合のため推奨

* * *
